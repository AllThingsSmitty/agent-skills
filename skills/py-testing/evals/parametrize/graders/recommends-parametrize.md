---
type: llm
criteria: |
  The agent should recommend `@pytest.mark.parametrize` as the solution:
  - Show or describe consolidating the cases into a single parametrized test
  - Include the password strings and expected bool as the parameter pairs
  - NOT just say "yes that's repetitive" without showing the pattern
---
