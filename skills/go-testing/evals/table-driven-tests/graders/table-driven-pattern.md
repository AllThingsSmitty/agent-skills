---
type: llm
---

The agent should rewrite the tests as a single table-driven test using a slice of structs (with fields for name, inputs, and expected output) and t.Run() for each case. The rewritten test should be clearly more maintainable than the original three separate functions.
