---
name: "ts-testing: introduces type-level testing"
tags: ["ts-testing", "types"]
runs: 3
max_turns: 6
---

I wrote a generic utility function and I want to make sure the return type is correct — not just that it returns the right value at runtime, but that the TypeScript type is exactly what I expect. How do I test types?
