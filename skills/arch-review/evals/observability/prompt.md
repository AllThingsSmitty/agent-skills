---
name: "arch-review: raises observability requirements proactively"
tags: ["arch-review", "observability"]
runs: 3
max_turns: 6
---

We're designing a new microservice to handle subscription billing. It will run as a separate service with its own database. What architectural concerns should we think through before building it?
