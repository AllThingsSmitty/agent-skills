---
type: llm
criteria: |
  The agent should show or describe writing a simple fake/stub struct that implements the interface (for tests), or mention using testify/mock to generate one. The fake should be simple enough to understand at a glance — a map or slice backing store is sufficient for most unit tests.
---
